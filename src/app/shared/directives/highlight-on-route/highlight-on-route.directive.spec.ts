import { HighlightOnRouteDirective } from './highlight-on-route.directive';

describe('HighlightOnRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnRouteDirective();
    expect(directive).toBeTruthy();
  });
});
