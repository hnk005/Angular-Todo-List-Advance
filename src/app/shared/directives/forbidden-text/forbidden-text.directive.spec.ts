import { ForbiddenTextDirective } from './forbidden-text.directive';

describe('ForbiddenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenTextDirective();
    expect(directive).toBeTruthy();
  });
});
